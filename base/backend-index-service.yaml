apiVersion: v1
kind: Service
metadata:
  name: mi-backend-index-service
spec:
  type: NodePort
  selector:
    app: mi-backend-index
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
